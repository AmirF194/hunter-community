# api 镜像构建上下文 · 只排**运行时确定用不到**的东西。
#
# ⚠️ 别往里加 app/ 、agents/ 、models/ 、sql/ 、data/ 、skills/ 、scripts/ ——
#    main.py 的 import 链和运行期读文件都会用到它们(sql/ 有启动时执行的语句,
#    data/ 有随代码分发的静态表)。CI 跑测试是在 checkout 上跑的,不在镜像里,
#    所以 tests/ 可以排。
__pycache__
**/__pycache__
*.py[cod]
*.egg-info
.pytest_cache
**/.pytest_cache
.mypy_cache
.ruff_cache
.git
.gitignore
.env
.env.*
!.env.example
tests
conftest.py
pytest.ini
.venv
venv
*.log
